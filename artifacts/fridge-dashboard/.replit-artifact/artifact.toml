kind = "web"
previewPath = "/"
title = "Smart Fridge Dashboard"
version = "1.0.0"
id = "artifacts/fridge-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19253

[services.development]
run = "pnpm --filter @workspace/fridge-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fridge-dashboard", "run", "build" ]
publicDir = "artifacts/fridge-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19253"
BASE_PATH = "/"
